{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && git log -1 --format=\"%h — %s\" | jq -Rs '{systemMessage: (\"📌 آخر commit: \" + rtrimstr(\"\\n\")), hookSpecificOutput: {hookEventName: \"PostToolUse\", additionalContext: (\"Latest commit after push: \" + rtrimstr(\"\\n\"))}}'",
            "timeout": 10,
            "statusMessage": "قراءة آخر commit…"
          }
        ]
      }
    ]
  }
}
